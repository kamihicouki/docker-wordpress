version: '2'

services:
  db:
    image: mariadb:latest
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: "yes"
    networks:
      - default
    expose:
      - "3306"
    volumes:
      - /var/lib/mysql
  app:
    image: wordpress:fpm
    networks:
      - default
    links:
      - "db:mysql"
    expose:
      - "9000"
    volumes:
      - /var/www/html
  web:
    image: nginx:latest
    networks:
      - default
    ports:
      - "80"
      - "443"
    volumes_from:
      - app:ro
